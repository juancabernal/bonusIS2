server:
  port: 8083

spring:
  application:
    name: uco-challenge-backend
  profiles:
    active: dev

  datasource:
    url: jdbc:postgresql://dpg-d3n8p524d50c73f4am9g-a.oregon-postgres.render.com/software2terraxs
    # username y password NO se definen aquí; vendrán del Key Vault en el bean
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
      naming:
        physical-strategy: org.hibernate.boot.model.naming.CamelCaseToUnderscoresNamingStrategy
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    open-in-view: false

  sql:
    init:
      mode: never

  jta:
    enabled: false

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://dev-qyb588fnz01iw3kn.us.auth0.com/  # TODO completar dominio Auth0 (con barra final)

logging:
  level:
    root: INFO
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql: TRACE
    com.azure.identity: INFO
    reactor.netty: WARN

jwt:
  expected-audience: https://uco-challenge-api            # TODO completar con el Identifier de Auth0

security:
  gateway:
    hmac-secret: ${GATEWAY_HMAC_SECRET:4F39B2E91A8D44C2B28C937A7F84E72F}

external:
  parameters:
    base-url: http://localhost:8080/parameters/api/v1/parameters
  messages:
    base-url: http://localhost:8081/messages/api/v1/messages

azure:
  keyvault:
    url: https://uco-vault-secrets.vault.azure.net/
  secrets:
    db-username: db-username
    db-url: db-url
    cliente-id: cliente-id
    cliente-secret-id: cliente-secret-id
    db-password: db-password

# ============================================================
# 📊 MÉTRICAS / PROMETHEUS / GRAFANA
# (Añadido sin modificar nada de lo anterior)
# ============================================================
management:
  metrics:
    tags:
      application: ${spring.application.name}
  endpoints:
    web:
      exposure:
        include: info,health,prometheus

# =========================
# Redis / Cache
# =========================
---
spring:
  cache:
    type: redis
    redis:
      time-to-live: 300000   # TTL por defecto para @Cacheable (5 min)
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}

uco:
  cache:
    users:
      ttl-seconds: 300       # TTL específico para cache de usuarios
    messages:
      ttl-seconds: 900
    parameters:
      ttl-seconds: 900

logging:
  level:
    org.springframework.cache: TRACE
    org.springframework.data.redis: DEBUG
